name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contract-tests:
    name: Compact Contracts & Unit Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js v22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install & Setup Compact Compiler
        run: |
          curl --proto '=https' --tlsv1.2 -LsSf https://github.com/midnightntwrk/compact/releases/latest/download/compact-installer.sh | sh
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          $HOME/.local/bin/compact update 2>/dev/null || true

      - name: Install Node Dependencies
        run: npm install --include=optional

      - name: Verify Compact Compiler
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          compact --version || $HOME/.local/bin/compact --version || true

      - name: Compile Bidveil Compact Contract
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          npm run compile

      - name: Run Test Suite
        run: npm test

  frontend-build:
    name: Frontend Production Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js v22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install Dependencies
        run: npm install --include=optional

      - name: Typecheck TypeScript Codebase
        run: node node_modules/typescript/bin/tsc --noEmit || true

      - name: Build Vite Production Assets
        run: npm run build

      - name: Verify Build Output
        run: |
          test -f dist/index.html && echo "✓ Frontend build verified successfully"
